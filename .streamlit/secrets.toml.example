# Streamlit Secrets Configuration Template
# Copy this file to .streamlit/secrets.toml for local development
# Add secrets directly in Streamlit Cloud dashboard for production

[api]
# Backend API URL - Update with your deployed backend
base_url = "https://multi-agent-healthcare-gl-1.onrender.com"

# Optional: Add authentication tokens if needed in the future
# api_key = "your-secret-api-key"
# auth_token = "your-auth-token"
